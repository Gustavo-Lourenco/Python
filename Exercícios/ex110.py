from moeda110 import moeda

valor = float(float(input('Digite o valor a ser calculado: ')))
desconto = int(input('Qual o desconto? '))
aumento = int(input('Qual é o aumento? '))

moeda(valor, desconto, aumento)
